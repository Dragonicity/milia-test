<div class="page-header">
  <%= render 'shared/page_title', title: t(:teacher_index_title) %>
  <ul class="actions form-inline">
    <li><%= link_to t(:new), new_teacher_path, class: "new" if logged_in?(:site_admin) %></li>
  </ul>
</div>
<br />
<div class="row row-offcanvas row-offcanvas-right">
  <div class="col-sm-10">
    <p class="float-right d-md-none">
      <button type="button" class="btn btn-primary btn-sm" data-toggle="offcanvas">Toggle nav</button>
    </p>
      <%= paginate @teachers %> <br />
      <div class="card-deck">
        <%= render @teachers %>
      </div>
      <%= paginate @teachers %>
  </div>
</div>