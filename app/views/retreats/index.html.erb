<div class="page-header">
  <%= render 'shared/page_title', title: t(:retreat_index_title) %>
  <ul class="actions form-inline">
    <li><%= link_to t(:new), new_retreat_path, class: "new" if logged_in?(:site_admin) %></li>
    <li><%= link_to t(:import), import_retreats_path, class: "import" if logged_in?(:site_admin) %></li>
  </ul>
</div>
<div class="container-fluid">
  <div class="row">
        <%= paginate @retreats %> <br /><br />
        <div class="card-deck">
          <%= render @retreats %>
        </div>
        <%= paginate @retreats %>
    </div>
  </div>
</div>